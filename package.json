{
  "name": "cfg",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@sondr3/prettier": "0.5.1",
    "prettier": "2.4.1"
  }
}
